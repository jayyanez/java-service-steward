# Third-party notices

Java Service Steward is distributed under the `Apache-2.0 OR MIT` license (see
`LICENSE-APACHE`, `LICENSE-MIT`, and `NOTICE`). The `wrapper.exe` executable
statically links the Rust crates listed below, each distributed under its own
license. For every license this file gives the crates that use it and the full
license text as shipped with those crates. `wrapper.jar` contains only the
project's own classes and depends on the Java runtime alone.

This file is generated by `cargo about generate about.hbs -o THIRD_PARTY_NOTICES.md`
from the dependencies recorded in `Cargo.lock` for the `x86_64-pc-windows-msvc`
target. Do not edit it by hand.

## Overview

| License | SPDX identifier | Crates |
| --- | --- | --- |
{{#each overview}}
| {{name}} | `{{id}}` | {{count}} |
{{/each}}

{{#each licenses}}
## {{name}}

SPDX identifier: `{{id}}`

Used by:

{{#each used_by}}
- `{{crate.name}}` {{crate.version}}
{{/each}}

```text
{{{text}}}
```

{{/each}}
